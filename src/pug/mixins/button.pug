mixin button(link = true)
	if (link)
		+e.A.button.h6.btn__(href='#')&attributes(attributes)
			+e.SPAN.circle
			+e.SPAN.text
				if block
					block
	else
		+e.BUTTON.button.h6.btn__()&attributes(attributes)
			+e.SPAN.circle
			+e.SPAN.text
				if block
					block